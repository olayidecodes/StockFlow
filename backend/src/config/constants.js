// Application constants
module.exports = {
    // Roles will be defined by user later
    // Placeholder structure for future implementation
    ROLES: {
        // To be filled when user provides role structure
    },

    // Pagination defaults
    DEFAULT_PAGE_SIZE: 20,
    MAX_PAGE_SIZE: 100,
};
